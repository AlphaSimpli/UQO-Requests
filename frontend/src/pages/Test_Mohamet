import React, { useState } from 'react';

export default function TestMohamet() {
    const [count, setCount] = useState(0);

    return (
        <div>
            <h1>Test Mohamet</h1>
            <p>Count: {count}</p>
            <button onClick={() => setCount(count + 1)}>
                Increment
            </button>
        </div>
    );
}